<Window x:Class="CCXboxController.Views.UsageOverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ShowActivated="False"
        Focusable="False"
        IsHitTestVisible="False"
        SizeToContent="WidthAndHeight"
        Opacity="0.78"
        WindowStartupLocation="Manual">
    <Border Background="#DD1A1A1A" CornerRadius="10" Padding="12,8" BorderBrush="#FF3A3A3A" BorderThickness="1">
        <StackPanel Orientation="Vertical" MinWidth="200">
            <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                <Ellipse Width="8" Height="8" Fill="#FF4FC3F7" VerticalAlignment="Center" Margin="0,0,6,0"/>
                <TextBlock Text="claudecode used" Foreground="#FFB0B0B0" FontSize="10" FontWeight="SemiBold" VerticalAlignment="Center"/>
            </StackPanel>
            <Grid Margin="0,2,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Grid.Column="0" Text="Model" Foreground="#FF888888" FontSize="11" Margin="0,0,10,2"/>
                <TextBlock Grid.Row="0" Grid.Column="1" x:Name="LblModel" Text="—" Foreground="White" FontSize="11" FontWeight="SemiBold" Margin="0,0,0,2"/>
                <TextBlock Grid.Row="1" Grid.Column="0" Text="Blok (5h)" Foreground="#FF888888" FontSize="11" Margin="0,0,10,2"/>
                <TextBlock Grid.Row="1" Grid.Column="1" x:Name="LblBlock" Text="—" Foreground="#FFFFD080" FontSize="11" FontWeight="SemiBold" Margin="0,0,0,2"/>
            </Grid>
            <ProgressBar x:Name="BlockProgress" Height="3" Margin="0,6,0,0" Minimum="0" Maximum="100" Value="0"
                         Background="#FF2A2A2A" Foreground="#FFFFC107" BorderThickness="0"/>
        </StackPanel>
    </Border>
</Window>
