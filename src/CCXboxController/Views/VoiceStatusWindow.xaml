<Window x:Class="CCXboxController.Views.VoiceStatusWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ShowActivated="False"
        Focusable="False"
        IsHitTestVisible="False"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="Manual">
    <Border Background="#EE1E1E1E" CornerRadius="14" Padding="14,8" BorderBrush="#FF3A3A3A" BorderThickness="1">
        <StackPanel Orientation="Horizontal">
            <!-- Recording: pulsing red dot -->
            <Ellipse x:Name="RecDot" Width="14" Height="14" Fill="#FFE53935" Margin="0,0,10,0" VerticalAlignment="Center" Visibility="Collapsed" RenderTransformOrigin="0.5,0.5">
                <Ellipse.RenderTransform>
                    <ScaleTransform x:Name="RecDotScale" ScaleX="1" ScaleY="1"/>
                </Ellipse.RenderTransform>
            </Ellipse>
            <!-- Transcribing: spinning blue arc -->
            <Grid x:Name="TransArc" Width="16" Height="16" Margin="0,0,10,0" VerticalAlignment="Center" Visibility="Collapsed">
                <Ellipse Width="16" Height="16" Stroke="#33FFFFFF" StrokeThickness="2"/>
                <Path Stroke="#FF4FC3F7" StrokeThickness="2" StrokeStartLineCap="Round" StrokeEndLineCap="Round" Data="M 8,0 A 8,8 0 0 1 16,8" RenderTransformOrigin="0.5,0.5">
                    <Path.RenderTransform>
                        <RotateTransform x:Name="ArcRotate" Angle="0" CenterX="8" CenterY="8"/>
                    </Path.RenderTransform>
                </Path>
            </Grid>
            <TextBlock x:Name="LblStatus" Text="" Foreground="White" FontSize="14" FontWeight="SemiBold" VerticalAlignment="Center"/>
        </StackPanel>
    </Border>
</Window>
