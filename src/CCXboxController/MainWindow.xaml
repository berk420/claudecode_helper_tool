<Window x:Class="CCXboxController.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Claude Code · Xbox Controller Companion"
        Height="720" Width="1300"
        WindowStartupLocation="CenterScreen"
        Icon="app.ico"
        Background="{StaticResource BgBrush}">
    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Top status bar -->
        <Border Grid.Row="0" Background="{StaticResource PanelBrush}" Padding="12" CornerRadius="6" Margin="0,0,0,10">
            <StackPanel Orientation="Horizontal">
                <Ellipse x:Name="ConnDot" Width="12" Height="12" Fill="#FFB94A4A" VerticalAlignment="Center"/>
                <TextBlock x:Name="ConnText" Text="Kontrolcü bağlı değil" Margin="8,0,16,0" VerticalAlignment="Center"/>
                <TextBlock Text="·" Foreground="{StaticResource MutedBrush}" Margin="0,0,16,0" VerticalAlignment="Center"/>
                <TextBlock Text="Son tetiklenen:" Foreground="{StaticResource MutedBrush}" VerticalAlignment="Center"/>
                <TextBlock x:Name="LastTrigger" Text="—" Margin="8,0,0,0" Foreground="{StaticResource AccentBrush}" FontWeight="Bold" VerticalAlignment="Center"/>
            </StackPanel>
        </Border>

        <!-- Main content: list + editor + subagents -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="280"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="280"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="{StaticResource PanelBrush}" Padding="8" CornerRadius="6" Margin="0,0,8,0">
                <DockPanel>
                    <TextBlock DockPanel.Dock="Top" Text="TUŞLAR" Foreground="{StaticResource MutedBrush}" FontWeight="Bold" Margin="4,4,0,8"/>
                    <ListBox x:Name="BindingList" SelectionChanged="BindingList_SelectionChanged" BorderThickness="0"/>
                </DockPanel>
            </Border>

            <Border Grid.Column="1" Background="{StaticResource PanelBrush}" Padding="16" CornerRadius="6">
                <Grid x:Name="EditorPanel">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock x:Name="SelectedKeyLabel" Text="Seçim yok" FontSize="20" FontWeight="Bold" Margin="0,0,0,12"/>
                    <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,12">
                        <TextBlock Text="Aksiyon tipi:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <ComboBox x:Name="ActionTypeBox" Width="260" SelectionChanged="ActionTypeBox_SelectionChanged">
                            <ComboBoxItem Content="Metin yaz"/>
                            <ComboBoxItem Content="Sesli yazım (push-to-talk)"/>
                            <ComboBoxItem Content="Seçimi sesli oku (TTS)"/>
                        </ComboBox>
                    </StackPanel>
                    <TextBlock x:Name="TextLabel" Grid.Row="2" Text="Yazılacak metin (\n = Enter)" Foreground="{StaticResource MutedBrush}" Margin="0,0,0,4"/>
                    <TextBox x:Name="TextBoxValue" Grid.Row="3" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" TextChanged="TextBoxValue_TextChanged"/>
                </Grid>
            </Border>

            <Border Grid.Column="2" Background="{StaticResource PanelBrush}" Padding="8" CornerRadius="6" Margin="8,0,0,0">
                <DockPanel>
                    <Grid DockPanel.Dock="Top" Margin="4,4,4,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="SUBAGENTS" Foreground="{StaticResource MutedBrush}" FontWeight="Bold" VerticalAlignment="Center"/>
                        <Button Grid.Column="1" Content="↻" Padding="6,0" FontSize="13" Click="RefreshSubagents_Click" ToolTip="Listeyi yenile"/>
                    </Grid>
                    <TextBlock x:Name="SubagentsEmptyHint"
                               DockPanel.Dock="Bottom"
                               Text=""
                               Foreground="{StaticResource MutedBrush}"
                               FontSize="11"
                               TextWrapping="Wrap"
                               Margin="4,6,4,4"
                               Visibility="Collapsed"/>
                    <ListBox x:Name="SubagentList"
                             BorderThickness="0"
                             HorizontalContentAlignment="Stretch"
                             ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="Padding" Value="4,2"/>
                                <Setter Property="ToolTipService.ShowDuration" Value="30000"/>
                                <Setter Property="ToolTip">
                                    <Setter.Value>
                                        <ToolTip MaxWidth="480">
                                            <StackPanel>
                                                <TextBlock Text="{Binding Name}" FontWeight="Bold" Margin="0,0,0,4"/>
                                                <TextBlock Text="{Binding Description}" TextWrapping="Wrap"/>
                                                <TextBlock Text="{Binding FilePath}" Foreground="#FF8A8A8A" FontSize="10" Margin="0,4,0,0" TextWrapping="Wrap"/>
                                            </StackPanel>
                                        </ToolTip>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ListBox.ItemContainerStyle>
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Margin="2,4">
                                    <TextBlock Text="{Binding Name}" FontWeight="Bold"/>
                                    <TextBlock Text="{Binding Description}"
                                               Foreground="#FF8A8A8A"
                                               FontSize="11"
                                               TextTrimming="CharacterEllipsis"
                                               TextWrapping="NoWrap"/>
                                    <StackPanel Orientation="Horizontal" Margin="0,2,0,0">
                                        <TextBlock Text="{Binding Model}" Foreground="{StaticResource AccentBrush}" FontSize="10"/>
                                        <TextBlock Text=" · " Foreground="#FF8A8A8A" FontSize="10"/>
                                        <TextBlock Text="{Binding Tools}" Foreground="#FF8A8A8A" FontSize="10" TextTrimming="CharacterEllipsis"/>
                                    </StackPanel>
                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </DockPanel>
            </Border>
        </Grid>

        <!-- Bottom settings panel -->
        <Border Grid.Row="2" Background="{StaticResource PanelBrush}" Padding="12" CornerRadius="6" Margin="0,10,0,0">
            <StackPanel>
                <!-- Row 1: general -->
                <Grid Margin="0,0,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Orientation="Horizontal">
                        <CheckBox x:Name="AutostartBox" Content="Windows ile birlikte başlat" VerticalAlignment="Center" Checked="AutostartBox_Toggle" Unchecked="AutostartBox_Toggle"/>
                        <TextBlock Text="·" Foreground="{StaticResource MutedBrush}" Margin="16,0" VerticalAlignment="Center"/>
                        <TextBlock Text="Dil:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <ComboBox x:Name="LangBox" Width="90" SelectionChanged="LangBox_SelectionChanged">
                            <ComboBoxItem Content="tr"/>
                            <ComboBoxItem Content="auto"/>
                            <ComboBoxItem Content="en"/>
                        </ComboBox>
                    </StackPanel>
                    <StackPanel Grid.Column="1" Orientation="Horizontal">
                        <Button Content="Overlay'i kapat" Click="CloseOverlay_Click"/>
                    </StackPanel>
                </Grid>

                <!-- TTS hız + ses -->
                <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                    <TextBlock Text="TTS hızı:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <ComboBox x:Name="TtsRateBox" Width="90" SelectionChanged="TtsRateBox_SelectionChanged">
                        <ComboBoxItem Content="0.75x"/>
                        <ComboBoxItem Content="1.0x"/>
                        <ComboBoxItem Content="1.25x"/>
                        <ComboBoxItem Content="1.5x"/>
                        <ComboBoxItem Content="1.75x"/>
                        <ComboBoxItem Content="2.0x"/>
                    </ComboBox>
                    <TextBlock Text="·" Foreground="{StaticResource MutedBrush}" Margin="16,0" VerticalAlignment="Center"/>
                    <TextBlock Text="Ses:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <Slider x:Name="TtsVolumeSlider" Width="200" Minimum="0" Maximum="100" TickFrequency="10" IsSnapToTickEnabled="False" VerticalAlignment="Center" ValueChanged="TtsVolumeSlider_ValueChanged"/>
                    <TextBlock x:Name="TtsVolumeLabel" Text="100" VerticalAlignment="Center" Margin="8,0,0,0" Width="34"/>
                    <Button Content="Sına" Margin="12,0,0,0" Padding="10,2" Click="TtsTest_Click"/>
                </StackPanel>

                <Separator Background="#FF3F3F46" Margin="0,4,0,8"/>

                <!-- Row 2: STT provider -->
                <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                    <TextBlock Text="Ses-metin sağlayıcı:" VerticalAlignment="Center" Margin="0,0,12,0" FontWeight="Bold"/>
                    <RadioButton x:Name="LocalRadio" Content="Yerel (Whisper)" GroupName="STT" VerticalAlignment="Center" Checked="ProviderChanged" Margin="0,0,16,0"/>
                    <RadioButton x:Name="OpenAiRadio" Content="OpenAI API (online)" GroupName="STT" VerticalAlignment="Center" Checked="ProviderChanged"/>
                </StackPanel>

                <!-- Local provider details -->
                <StackPanel x:Name="LocalPanel" Orientation="Vertical">
                    <TextBlock Text="Yerel model:" Foreground="{StaticResource MutedBrush}" Margin="0,0,0,4"/>
                    <StackPanel x:Name="ModelRadios" Orientation="Vertical" Margin="0,0,0,8"/>
                    <WrapPanel Orientation="Horizontal" ItemHeight="32">
                        <TextBlock x:Name="ModelStatus" Text="bilinmiyor" VerticalAlignment="Center" Margin="0,0,12,0"/>
                        <Button x:Name="DownloadButton" Content="Modeli indir" Margin="0,0,8,0" Click="DownloadButton_Click"/>
                        <Button x:Name="CalibrateButton" Content="Ses Kalibrasyonu" Margin="0,0,16,0" Click="CalibrateButton_Click"/>
                        <TextBlock Text="Initial prompt:" Foreground="{StaticResource MutedBrush}" VerticalAlignment="Center" Margin="0,0,6,0"/>
                        <TextBlock x:Name="InitialPromptPreview" Text="—" VerticalAlignment="Center" MaxWidth="280" TextTrimming="CharacterEllipsis"/>
                    </WrapPanel>
                </StackPanel>

                <!-- OpenAI provider details -->
                <Grid x:Name="OpenAiPanel" Visibility="Collapsed">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="API key:" Foreground="{StaticResource MutedBrush}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <PasswordBox x:Name="ApiKeyBox" Grid.Column="1" Background="#FF1E1E1E" Foreground="{StaticResource TextBrush}" BorderBrush="#FF3F3F46" Padding="6,4" PasswordChanged="ApiKeyBox_Changed"/>
                    <TextBlock Grid.Column="2" Text="Model:" Foreground="{StaticResource MutedBrush}" VerticalAlignment="Center" Margin="16,0,8,0"/>
                    <ComboBox x:Name="OpenAiModelBox" Grid.Column="3" Width="210" SelectionChanged="OpenAiModelBox_Changed">
                        <ComboBoxItem Content="gpt-4o-transcribe"/>
                        <ComboBoxItem Content="gpt-4o-mini-transcribe"/>
                        <ComboBoxItem Content="whisper-1"/>
                    </ComboBox>
                    <Button Grid.Column="4" Content="Bağlantıyı test et" Margin="12,0,0,0" Click="TestOpenAi_Click"/>
                </Grid>

                <Separator Background="#FF3F3F46" Margin="0,8,0,8"/>

                <!-- Row 3: Claude Code usage overlay -->
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Kullanım göstergesi:" VerticalAlignment="Center" Margin="0,0,12,0" FontWeight="Bold"/>
                    <CheckBox x:Name="UsageOverlayBox" Content="Sol alt köşede 'claudecode used' overlay'ini göster" VerticalAlignment="Center" Checked="UsageOverlayBox_Toggle" Unchecked="UsageOverlayBox_Toggle"/>
                    <TextBlock Text="·" Foreground="{StaticResource MutedBrush}" Margin="16,0" VerticalAlignment="Center"/>
                    <TextBlock x:Name="UsageOverlayStatus" Text="—" Foreground="{StaticResource MutedBrush}" VerticalAlignment="Center"/>
                </StackPanel>

                <!-- Row 4: Chat end sound notification -->
                <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                    <TextBlock Text="Chat sonu ses bildirimi:" VerticalAlignment="Center" Margin="0,0,12,0" FontWeight="Bold"/>
                    <CheckBox x:Name="ChatEndSoundBox" Content="Claude Code her cevap verdiğinde ses çal" VerticalAlignment="Center" Checked="ChatEndSoundBox_Toggle" Unchecked="ChatEndSoundBox_Toggle"/>
                    <Button Content="Test" Margin="12,0,0,0" Padding="10,2" Click="ChatEndSoundTest_Click"/>
                    <TextBlock Text="·" Foreground="{StaticResource MutedBrush}" Margin="16,0" VerticalAlignment="Center"/>
                    <TextBlock x:Name="ChatEndSoundStatus" Text="—" Foreground="{StaticResource MutedBrush}" VerticalAlignment="Center"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</Window>
